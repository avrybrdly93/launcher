name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium firefox

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Import boundaries
        run: pnpm lint:deps

      - name: Test
        run: pnpm test

      - name: Benchmark regression check (soft warn)
        run: pnpm bench:solverkit

      - name: Cross-engine drift check (soft warn)
        # --record so the committed results file is refreshed from the runner,
        # where Playwright's browsers are actually installed. Without the flag
        # the script measures and reports but writes nothing (P0.102), which is
        # what a local pre-push gate run should do.
        run: pnpm check:cross-engine-drift --record

      - name: Engine API docs
        run: pnpm --filter @ballista/engine run docs

      - name: SolverKit API docs
        run: pnpm --filter @ballista/solverkit run docs

      - name: Build app
        run: pnpm --filter @ballista/app build

      - name: Bundle size budget
        run: pnpm --filter @ballista/app check-bundle-size

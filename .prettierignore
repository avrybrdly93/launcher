dist/
node_modules/
pnpm-lock.yaml
ballista-technical-blueprint.md
packages/*/src/*.derivation.md
# P4.40: generated by scripts/generate-physics-docs.mjs from the blueprint's §3 prose
# verbatim. Prettier reflows the LaTeX in it exactly as it would in the blueprint above,
# which is why that file is already exempt; formatting these would also make the
# generator's --check comparison fail against its own output.
docs/physics/*.md
